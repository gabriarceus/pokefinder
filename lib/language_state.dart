import 'package:flutter/material.dart';

/// Questa classe contiene lo stato della lingua dell'applicazione
class LanguageState {
  static final ValueNotifier<bool> languageNotifier = ValueNotifier<bool>(true);
}